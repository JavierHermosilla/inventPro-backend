export const ROLES = {
  ADMIN: 'admin',
  USER: 'user',
  MANAGER: 'manager',
  BODEGUERO: 'bodeguero'
}

export const ALL_ROLES = [ROLES.ADMIN, ROLES.USER, ROLES.MANAGER, ROLES.BODEGUERO]
export const ADMIN_ROLES = [ROLES.ADMIN]
export const USER_ROLES = [ROLES.USER, ROLES.MANAGER]
