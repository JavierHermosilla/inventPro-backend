export const ROLES = {
  ADMIN: 'admin',
  USER: 'user',
  MANAGER: 'manager'
}

export const ALL_ROLES = [ROLES.ADMIN, ROLES.USER, ROLES.MANAGER]
export const ADMIN_ROLES = [ROLES.ADMIN]
export const USER_ROLES = [ROLES.USER, ROLES.MANAGER]
